# 开发期 --patch 覆盖层（绝对路径加载本地插件）。
# 打包安装时请用包内的 cordis.patch.yml（按包名 dsh-rembg 解析）。
- insert:
    - id: rembg
      name: '/root/test2/rembg-plugin/index.js'
      config:
        model: u2net
        timeoutMs: 600000
        autoInstall: true
